# FeedLens Backend

Minimal Express backend used for `/health` and `/score`.

## Setup

```bash
npm install
```

## Run

```bash
npm start
```

Server runs at `http://localhost:3001`.

## Test

```bash
node test-api.js
```

## OpenRouter model

This backend is pinned to Elephant only:

- `openrouter/elephant-alpha`

